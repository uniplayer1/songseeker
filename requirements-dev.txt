# Development / testing / linting dependencies for SongSeeker tools
# Install with: pip install -r requirements-dev.txt

# Core runtime (also in requirements.txt)
-r tools/requirements.txt

# Linting & formatting (modern & fast)
ruff>=0.5.0
black>=24.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional: for type checking the python tools
# mypy>=1.10.0
# pandas-stubs
